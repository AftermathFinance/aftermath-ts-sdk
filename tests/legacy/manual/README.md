# Manual and historical tests

These scripts are retained as reference material only. They depend on built
files in `dist/`, are not part of the Jest suite, and may target APIs that have
since changed. Add a deterministic regression to the source-mirrored Jest
suite before relying on it for CI coverage.
