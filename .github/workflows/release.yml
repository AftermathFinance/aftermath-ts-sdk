name: Release

on:
    push:
        branches:
            - main

concurrency:
    group: ${{ github.workflow }}-${{ github.ref }}
    cancel-in-progress: false

permissions:
    contents: read

jobs:
    verify:
        name: Verify SDK before publishing
        runs-on: ubuntu-latest
        timeout-minutes: 20
        permissions:
            contents: read
        steps:
            - uses: actions/checkout@v4

            - uses: oven-sh/setup-bun@v2
              with:
                  bun-version: 1.3.8

            - uses: actions/setup-node@v4
              with:
                  node-version: 22

            - name: Install
              run: bun install --frozen-lockfile

            - name: Test with coverage thresholds
              run: bun run test:ci

            - name: Typecheck tests
              run: bun run typecheck:tests

            - name: Audit test surface
              run: bun run test:surface:strict

            - name: Test idempotent release guard
              run: bun run test:release

            - name: Build and verify package
              run: |
                  bun run build
                  bun run verify:public
                  bun run package:check

    release:
        name: Release (changesets)
        needs: verify
        runs-on: ubuntu-latest
        timeout-minutes: 15
        permissions:
            contents: write
            pull-requests: write
            id-token: write
        steps:
            - uses: actions/checkout@v4

            - uses: oven-sh/setup-bun@v2
              with:
                  bun-version: 1.3.8

            - uses: actions/setup-node@v4
              with:
                  node-version: 22
                  registry-url: https://registry.npmjs.org

            # Trusted publishing needs npm >= 11.5.1. Pin a known npm 11 release
            # so Changesets does not inherit breaking npm CLI behavior from latest.
            - name: Install pinned npm for OIDC trusted publishing
              run: npm install -g npm@11.6.2

            - name: Install
              run: bun install --frozen-lockfile

            - name: Build and verify package before release
              run: |
                  bun run build
                  bun run verify:public
                  bun run package:check

            - name: Create release PR or publish
              uses: changesets/action@v1
              with:
                  publish: bun run release
                  version: bun run version-packages
                  title: "chore: version packages"
                  commit: "chore: version packages"
              env:
                  GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
                  NPM_CONFIG_PROVENANCE: true

    publish-dev:
        name: Publish @dev snapshot
        needs: verify
        runs-on: ubuntu-latest
        timeout-minutes: 15
        permissions:
            contents: read
            id-token: write
        steps:
            - uses: actions/checkout@v4

            - uses: oven-sh/setup-bun@v2
              with:
                  bun-version: 1.3.8

            - uses: actions/setup-node@v4
              with:
                  node-version: 22
                  registry-url: https://registry.npmjs.org

            # Trusted publishing needs npm >= 11.5.1. Keep the dev publisher on
            # the same known npm version as the stable release publisher.
            - name: Install pinned npm for OIDC trusted publishing
              run: npm install -g npm@11.6.2

            - name: Install
              run: bun install --frozen-lockfile

            - name: Compute dev version
              id: dev-version
              run: |
                  base_version="$(node -p "require('./package.json').version")"
                  dev_version="${base_version}-dev.$(git rev-parse --short HEAD)"
                  echo "version=${dev_version}" >> "$GITHUB_OUTPUT"
                  echo "Publishing dev snapshot ${dev_version}"

            - name: Apply dev version
              env:
                  DEV_VERSION: ${{ steps.dev-version.outputs.version }}
              run: node -e "const fs = require('node:fs'); const path = 'package.json'; const manifest = JSON.parse(fs.readFileSync(path, 'utf8')); manifest.version = process.env.DEV_VERSION; fs.writeFileSync(path, JSON.stringify(manifest, null, 2) + '\n');"

            - name: Build
              run: bun run build

            - name: Verify dev package before publish
              run: |
                  bun run verify:public
                  bun run package:check

            - name: Publish to npm under the dev tag
              env:
                  NPM_CONFIG_PROVENANCE: true
              run: npm publish --tag dev --access public
